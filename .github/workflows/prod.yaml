name: Firebase Cloud Functions Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Install required packages for source code
        run: npm install

      - name: Authenticate with Firebase
        run: firebase login:ci --no-localhost --token ${{ secrets.FIREBASE_TOKEN }}

      - name: Deploy backend to Firebase
        run: firebase deploy